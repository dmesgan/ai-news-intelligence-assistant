spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ainews
    username: ainews
    password: ainews
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.mesgan.ainews: DEBUG
    org.hibernate.SQL: DEBUG
